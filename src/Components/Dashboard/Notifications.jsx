import React from 'react'
import { NotificationItem, NotificationBar} from './DashboardComponents'

const Notifications = () => {
    // Create an array of Notification Item's to plug into the Notification Bar
    return (
        <div className="Notifications">
            <NotificationBar notifications={} />
        </div>
    )
}